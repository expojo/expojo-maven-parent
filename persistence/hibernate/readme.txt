Hibernate classes have been implemented but have not been fully tested.

To build make sure you change the build file so that the hibernateDir variable points to the directory on your system containing the Hibernate jar file(s).